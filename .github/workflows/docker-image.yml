name: Docker Image CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        echo ${{ secrets.DOCKER_PASSWORD }} | docker login --username michabirklbauer --password-stdin
        docker build . --file Dockerfile --tag michabirklbauer/internalionsv2:latest
        docker push michabirklbauer/internalionsv2:latest
        GIT_SHA="$(git rev-parse --short HEAD)"
        docker tag michabirklbauer/internalionsv2:latest michabirklbauer/internalionsv2:$GIT_SHA
        docker push michabirklbauer/internalionsv2:$GIT_SHA
